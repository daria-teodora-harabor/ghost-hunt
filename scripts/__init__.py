"""Entry-point scripts. Run as modules from the repository root so that `src` is
importable: `python -m scripts.build_population` (running the file directly puts
the script's own directory on sys.path instead of the repo)."""
